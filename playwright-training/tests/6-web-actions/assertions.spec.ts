//Assertions : Assertions are default method provided by playwright to compare expected results vs acutal results

//There are 2 types :
//1. Soft Assertions : When assertions are failed still test execution continued and marked as failed at the end of the test execution.
//Syntax : expect(actual).tobe(expected);

//2. Hard Assertions : When assertions are failed immediately it stops the test execution and marked it as failed.
//syntax : expect.soft(actual).tobe(expected);

